{
  "name": "inventory-consumer-app",
  "version": "1.0.0",
  "description": "Inventory management and consumer ordering application.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write \"**/*.{html,js,css,json,md}\"",
    "build:css": "postcss src/input.css -o dist/output.css",
    "watch:css": "postcss src/input.css -o dist/output.css --watch",
    "build": "npm-run-all build:css",
    "start": "echo \"No start script defined yet. Serve HTML files directly.\""
  },
  "keywords": [
    "inventory",
    "pwa",
    "ecommerce"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^8.0.0",
    "eslint-plugin-prettier": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.4.0",
    "postcss-cli": "^9.0.0",
    "prettier": "^2.5.0",
    "tailwindcss": "^3.0.0"
  },
  "dependencies": {}
}
